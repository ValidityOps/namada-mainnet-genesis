name: Process Bond PRs into valid toml
on:
  pull_request:
    types:
      - opened
permissions:
  contents: write
jobs:
  process_pr:
    runs-on: ubuntu-latest
    steps:
      - name: Create clean branch
        uses: peterjgrainger/action-create-branch@v2.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          branch: 'patch-${{ github.event.pull_request.number }}'
          sha: '6649294b1ab42131ffcdf643a7e1c985d29addf2'
      - name: Checkout clean branch first
        uses: actions/checkout@v4
        with:
          ref: 'patch-${{ github.event.pull_request.number }}'
      - name: Checkout the PR branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr checkout ${{ github.event.pull_request.number }}
      - name: Process files changed in the current commit
        id: read-file
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          changedFiles=$(git diff --name-only HEAD^)
          for file in $changedFiles; do
            echo "Processing file: $file"
            if [[ $file == *.toml ]]
            then
              content=`cat $file`;
              echo content=$content >> $GITHUB_OUTPUT;
              echo filename=$file >> $GITHUB_OUTPUT;
            fi
          done
      - name: Output the changes on the clean branch
        run: |
          git switch 'patch-${{ github.event.pull_request.number }}'
          echo ${{ steps.read-file.outputs.content }} | base64 -d > ${{ steps.read-file.outputs.filename }}
          git add ${{ steps.read-file.outputs.filename }}
          git commit -a -m '${{ github.event.pull_request.title }}'
          git push
      - name: Create Upstream Pull Request
        env:
          GITHUB_TOKEN: ${{ secrets.SERVICE_TOKEN }}
        run: |
          PR_EXISTS=$(gh pr list --repo anoma/namada-mainnet-genesis --head '${{ github.repository_owner }}:patch-${{ github.event.pull_request.number }}' --json number --jq '.[].number')
          if [ -z "$PR_EXISTS" ]; then
            gh pr create \
              --title '${{ github.event.pull_request.title }}' \
              --body '${{ github.event.pull_request.body }}' \
              --head '${{ github.repository_owner }}:patch-${{ github.event.pull_request.number }}' \
              --base main \
              --repo anoma/namada-mainnet-genesis
          else
            echo "PR already exists: $PR_EXISTS"
          fi
      - name: Delete PR Branch to Cleanup
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git branch -D ${{ github.event.pull_request.head.ref }}
